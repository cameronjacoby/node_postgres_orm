<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <h1>News Feed</h1>
    <% people.forEach(function(person) { %>
      <p><a href="/people/<%= person.id %>"><%= person.firstname %> <%= person.lastname %></a></p>
    <% }); %>
  </body>
</html>